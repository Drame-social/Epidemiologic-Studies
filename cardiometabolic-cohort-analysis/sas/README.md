# SAS workflow
Edit `%let root` in `sas/config.sas`, then submit `sas/run_all.sas` **from the repository root** in SAS 9.4 / SAS Studio. run_all includes config first (which defines &root), then programs 01-11. Run locally (needs SAS).
